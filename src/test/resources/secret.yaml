apiVersion: v1
kind: Secret
metadata:
  # The name must match what you reference in your Deployment.
  name: shared-access-token
  # namespace: your-app-namespace
type: Opaque
# The 'data' field is intentionally omitted.
# Your leader pod will add data to this secret at runtime.