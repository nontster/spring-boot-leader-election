apiVersion: v1
kind: Service
metadata:
  name: leader-election-demo
spec:
  selector:
    app: leader-election-demo
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
