apiVersion: apps/v1
kind: Deployment
metadata:
  name: leader-election-demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: leader-election-demo
  template:
    metadata:
      labels:
        app: leader-election-demo
    spec:
      serviceAccountName: leader-election-sa
      containers:
      - name: leader-election-demo
        image: nontster/spring-leader:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
